<% if logged_in? %>
  <form method="GET" action="/">
    <input class="myButton" type="submit" value="Scry"/>
  </form>

  <form method="GET" action="/users">
    <input class="myButton" type="submit" value="Leaderboard"/>
  </form>

  <p>Hello, <%= @current_user.username %>! You have scried <%= @current_user.score %> cards.</p>

  <h1>Favorite Cards</h1>
  <ul>
    <% @current_user.favorites.each do |favorite| %>
      <li><img id="card_image" src="<%= display_image(favorite.id) %>" /></li>
    <% end %>
  </ul>
<% end %>